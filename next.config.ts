import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Il badge di sviluppo di Next si sovrappone all'angolo in basso a
  // sinistra, dove vive la barra dei valori del brand.
  devIndicators: false,
};

export default nextConfig;
